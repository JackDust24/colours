export enum ColourType {
  RGB = "RGB",
  RGBA = "RGBA",
  HSL = "HSL",
  HSLA = "HSLA",
  HEX = "HEX",
}

export enum ColourDefaultType {
  rgb = "rgb",
  hsl = "hsl",
}

export enum RandomDefaultType {
  hue = "hue",
  percentage = "percentage",
  alpha = "alpha",
  colour = "colour",
}
